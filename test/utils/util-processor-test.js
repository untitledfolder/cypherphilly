const { expect } = require('chai');

const { Processor } = require('../../utils/processor');

describe('Processor', () => {
  describe('Init', () => {
    it('should exist', () => {
    });
  });
});
