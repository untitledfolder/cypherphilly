server {
  listen 80;

  root /opt/www/cypherphilly;

  index index.html;

  server_name cypherphil.ly www.cypherphil.ly;

  location / {
    try_files $uri $uri/ /index.html =404;
  }

  location /api {
    rewrite ^/api(.*) $1 break;
    proxy_pass http://127.0.0.1:3002;
  }
}
